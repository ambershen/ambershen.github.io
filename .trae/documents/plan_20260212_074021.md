I will add a horizontal scrolling image carousel to each project in the "Code & Product" section.

1.  **Modify `index.html`**:
    -   Locate the "Code + Product" section (`<article class="brain-panel left-brain">`).
    -   For each project (`Dirty Window`, `PaletteMe`, `Birth Crystal`, `Small Tool Bento Box`), I will inject a new `<div class="project-carousel">` container inside the project's `.cmd-output` block.
    -   Inside each carousel, I will add `<img>` tags for the project screenshots found in the `images/` directory.
    -   **Image Order**: `frontpage.png` first, followed by `1st.png`, `2nd.png`, `3rd.png`, etc., as requested.
    -   **Paths**:
        -   `images/dirty-window/` (4 images)
        -   `images/palette-me/` (4 images)
        -   `images/birth-crystal/` (4 images)
        -   `images/small-tool-bento-box/` (7 images)

2.  **Update `styles.css`**:
    -   Add CSS classes for `.project-carousel` to enable horizontal scrolling (using CSS Grid/Flexbox and Scroll Snap).
    -   Style the images to have a consistent height (`200px`) and ensure they resize responsively.
    -   Hide the scrollbar for a cleaner look while maintaining scroll functionality.

**Outcome**: Each project in the terminal list will display a swipeable/scrollable strip of screenshots below its description and metadata.